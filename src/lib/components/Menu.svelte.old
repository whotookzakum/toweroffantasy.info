<menu class="full-bleed">
    <slot />
</menu>

<style>
    menu {
        padding: 0;
        list-style: none;
        margin: 1rem;
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(128px, 1fr));
        gap: 0.5rem;
    }
</style>