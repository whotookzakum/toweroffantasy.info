query FullEquipment($id: String!) {
    gear(id: $id) {
        ...equipmentEntry
        baseStat {
            propName
            propValue
            icon
            name
        }
        statPool {
            propName
            weightValue
            icon
            name
        }
    }
}