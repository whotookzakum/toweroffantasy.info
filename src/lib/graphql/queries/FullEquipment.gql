query FullEquipment($id: String!) {
    gear(id: $id) {
        ...equipmentEntry
        baseStat {
            propName
            propValue
        }
        statPool {
            propName
            weightValue
        }
    }
}