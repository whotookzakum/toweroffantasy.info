query FullEquipment($id: String!) {
    gear(id: $id) {
        ...equipmentEntry
        maxLevel
        advancement {
            amount
            icon
            matId
            name
            rarity
        }
        advancementExp
        baseStat {
            propName
            propValue
            icon
            name
        }
        baseUpgradeProps {
            multiplier
            propId
        }
        expValue
        matList
        statPool {
            propName
            weightValue
            icon
            name
        }
        props {
            PropId
            PropInitValue
            PropMaxValue
            PropMinValue
            Quality
        }
    }
}