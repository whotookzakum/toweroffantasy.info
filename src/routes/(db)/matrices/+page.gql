query Matrices($lang: String) {
    matrices(lang: $lang) {
        rarity
        name
        id
        assets {
            iconLarge
        }
        Banners {
            bannerNo
        }
        __typename
    }
}